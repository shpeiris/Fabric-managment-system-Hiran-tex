diff --git a/backend/db/schema.sql b/backend/db/schema.sql
index e6934d4..28a7854 100644
--- a/backend/db/schema.sql
+++ b/backend/db/schema.sql
@@ -69,7 +69,7 @@ CREATE TABLE fabrics (
 -- 5. Cart Table
 CREATE TABLE cart (
     cart_id SERIAL PRIMARY KEY,
-    user_id INTEGER NOT NULL REFERENCES customers(customer_id) ON DELETE CASCADE,
+    customer_id INTEGER NOT NULL REFERENCES customers(customer_id) ON DELETE CASCADE,
     fabric_id INTEGER NOT NULL REFERENCES fabrics(fabric_id) ON DELETE CASCADE,
     quantity DECIMAL(10, 2) NOT NULL DEFAULT 1.0,
     added_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
@@ -157,7 +157,7 @@ CREATE TABLE confirmation_logs (
 -- 12. Activity Logs Table (Enhanced for Sales Order Management)
 CREATE TABLE activity_logs (
     log_id SERIAL PRIMARY KEY,
-    user_id INTEGER,
+    customer_id INTEGER REFERENCES customers(customer_id) ON DELETE SET NULL,
     action_type VARCHAR(50) NOT NULL,
     description TEXT,
     target_id INTEGER,
